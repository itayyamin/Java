public class GeneralQuestions
{

    //Return a random element from thr given array
    public static int randomElement(int[] arr) {
        return -1;
    }

    //Return true if the array elements are sorted in increasing order, false otherwise
    public static boolean isSorted(int[] arr) {
        return false;
    }

    //Returns a one-dimensional array containing the averages of the columns of hte given table ( a two-dimensional array)
    public static double[] colAverages(int[][] table) {
        return null;
    }

    //Return the maximum value in the given array
    //For example if the given array is [3,5,1,5,2,4] return 5
    public static int max(int[] arr) {
        return -1;
    }

    //Returns the log of x according to base 2
    // assumes that x is power of 2
    // implement recursively
    public static int log(int x) {
        return -1;
    }


    public static int binarySearch(int arr[], int x) {
        return binarySearch(arr, 0, arr.length - 1, x);
    }
    //Return the index of x if it is present in arr[low..high], otherwise return -1, using binary search
    // assume the given array is sorted in increasing order
    public static int binarySearch(int arr[], int low, int high, int x){
        return -1;
    }


    public static void main(String[] args) {}


}
